apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-eventer.fullname" . }}-targets
  labels:
    {{- include "ai-eventer.labels" . | nindent 4 }}
data:
  targets.yaml: |
    {{- toYaml .Values.watchTargets | nindent 4 }}
